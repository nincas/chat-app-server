{
  "name": "react-server",
  "version": "1.0.0",
  "description": "react express server",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": "nodemon app",
    "react": "npm run start --prefix ../client-app",
    "app": "concurrently \"npm run server\" \"npm run react\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.18.3",
    "concurrently": "^4.1.0",
    "cors": "^2.8.5",
    "env": "0.0.2",
    "express": "^4.16.4",
    "socket.io": "^2.2.0"
  }
}